# ===== 部署指南 =====

# 1. 数据库（必填，Vercel 自动注入）
# 在 Vercel 项目页面 → Storage → Create Database → Postgres
# 创建后自动注入以下变量，无需手动填写：
# POSTGRES_URL=postgres://...
# POSTGRES_POOL_URL=postgres://...
# 代码首次运行自动建表

# 2. JWT 密钥（可选，不填则自动从数据库生成并持久化）
# 建议填写以提升首次启动性能
# JWT_SECRET=your_custom_secret_here

# 注意：
# - JWT_SECRET 不填时，系统自动生成 64 字节随机密钥并存入数据库
# - 修改 JWT_SECRET 后，之前加密的 GitHub Token 将无法解密，需重新填写
# - 管理员默认账号：admin，默认密码：Admin@123456
#   首次登录后请立即在管理后台修改密码！

# ===== 功能说明 =====
# - 动态版本号：管理员可在后台「系统设置」页面更新版本号，所有用户下次访问自动刷新
# - 单会话登录：同一账号只允许一处登录，新登录会使旧会话自动失效
# - 健康检查：管理员后台「系统设置」页面可运行数据库连接和环境变量检查
# - API 函数数量：共 8 个 Serverless 函数（限制 12 个以内）
#   1. api/auth/[action].js  - 认证、注册、版本号、健康检查
#   2. api/admin/[action].js - 管理员后台所有操作
#   3. api/config.js         - 仓库管理、下载
#   4. api/upload.js          - 文件上传
#   5. api/history.js         - 上传历史
#   6. api/oauth.js           - GitHub OAuth
#   7. api/webhook.js         - Webhook 通知
#   8. api/team/[action].js   - 团队协作
